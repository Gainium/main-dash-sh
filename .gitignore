node_modules/
*.tsbuildinfo

# env
.env
.env.development
.env.production
.env.local

.eslintcache
# Playwright unit/e2e run output
test-results/
playwright-report/

# Playwright auth state (playwright.config.ts -> use.storageState).
# This is a real session storage state (cookies/JWT) and this repo is public.
# Scope to the .auth dir only, never the whole tests/ dir (see main-dash-redesign).
tests/.auth/
